# Docker-Project-2
Create a small database with 2-3 tables (e.g., users, orders, products) using PostgreSQL, manage it via PGAdmin, and use Python to insert, query, and clean data. All services will run in Docker containers using Docker Compose.
